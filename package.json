{
  "name": "calmlist",
  "private": true,
  "version": "1.0.0",
  "description": "A calm, keyboard-first task manager inspired by Todoist.",
  "workspaces": [
    "web",
    "server"
  ],
  "scripts": {
    "dev": "npm run dev --workspace web",
    "dev:server": "npm run dev --workspace server",
    "build": "npm run build --workspace web && npm run build --workspace server",
    "start": "npm run start --workspace server",
    "test": "npm test --workspace web && npm test --workspace server && npm run test:db",
    "typecheck": "npm run typecheck --workspace web && npm run typecheck --workspace server",
    "supabase:selfhost": "node scripts/supabase-selfhost.mjs",
    "supabase:link": "npx supabase link",
    "supabase:push": "npx supabase db push",
    "test:db": "node --test supabase/tests/isolation.test.mjs"
  },
  "engines": {
    "node": ">=22.5"
  },
  "license": "MIT",
  "devDependencies": {
    "@electric-sql/pglite": "^0.5.8"
  }
}
