# Port the API (and bundled web app) listens on
PORT=8787
# SQLite file; mount a volume here in production
DATABASE_PATH=calmlist.db
# Comma-separated origins allowed to call the API, e.g. https://corund207.github.io
CORS_ORIGIN=*
