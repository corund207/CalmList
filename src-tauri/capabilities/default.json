{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "The only native calls the app may make: open http(s) and mailto links in the system browser.",
  "windows": ["main"],
  "permissions": ["core:default", "opener:default"]
}
